---
language: python
python: "2.7"
services:
- docker

env:
  matrix:
  - ANSIBLE_VERSION=1.9.4 ANSIBLE_ROLE_FILE=common.yml
  - ANSIBLE_VERSION=1.9.4 ANSIBLE_ROLE_FILE=ruby.yml
  - ANSIBLE_VERSION=2.1.2 ANSIBLE_ROLE_FILE=common.yml
  - ANSIBLE_VERSION=2.1.2 ANSIBLE_ROLE_FILE=ruby.yml

before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq python-apt python-pycurl

install:
  - pip install ansible=="$ANSIBLE_VERSION"

script:
  - echo localhost > inventory
  - ansible-playbook -i inventory tests/$ANSIBLE_ROLE_FILE --syntax-check
  - ansible-playbook -i inventory tests/$ANSIBLE_ROLE_FILE --connection=local --sudo
  - >
    ansible-playbook -i inventory tests/$ANSIBLE_ROLE_FILE --connection=local --sudo
    | grep -q '.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)
